import { Container, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Container>
      <Typography variant="h4">Dashboard</Typography>
      {/* Overview UI will go here */}
    </Container>
  );
};

export default Dashboard;
